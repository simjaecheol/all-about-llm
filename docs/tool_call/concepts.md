---
title: Tool Call 개념과 기초
parent: Tool Call
nav_order: 1
---

# Tool Call 개념과 기초

## Tool Call이란?

**Tool Call**은 대형 언어 모델(LLM)이 외부 도구나 API와 상호작용하여 자신의 한계를 극복하고 더 정확하고 유용한 응답을 생성할 수 있게 하는 핵심 기능입니다.

## 왜 Tool Call이 필요한가?

전통적인 LLM은 훈련 데이터에 포함된 지식만을 활용할 수 있어 여러 한계가 있었습니다:

- **훈련 시점 이후 정보 부족**: 모델이 훈련된 시점 이후의 최신 정보나 사건을 알지 못함
- **수학적 계산의 부정확성**: 확률적 텍스트 생성으로 인한 산술 연산 오류
- **사실 정보 부정확성**: 모델이 그럴듯하지만 잘못된 정보를 생성하는 할루시네이션 현상
- **동적 데이터 접근 불가**: 실시간 주가, 날씨, 데이터베이스 정보 등에 접근할 수 없음

## Tool Call 작동 원리

Tool Call은 다음과 같은 단계로 작동합니다:

1. **사용자 요청 분석**: LLM이 요청을 분석하여 외부 도구가 필요한지 판단
2. **도구 선택**: 미리 정의된 도구 목록에서 적절한 도구를 선택
3. **매개변수 생성**: 도구 실행에 필요한 매개변수를 JSON 형태로 구조화
4. **도구 실행**: 실제 도구가 실행되어 결과를 반환 (LLM이 직접 실행하지 않음)
5. **응답 생성**: 도구 실행 결과를 바탕으로 최종 응답 생성

![Tool Call 작동 원리](../assets/images/tool_call_flow_diagram.png)

## Function Calling과 Tool Calling의 차이점

혼용되어 사용되는 경우가 많지만, 실제로는 미묘한 차이가 있습니다:

| 구분 | Function Calling | Tool Calling |
|------|-----------------|-------------|
| **범위** | 단일 함수 호출에 중점 | 여러 도구/함수를 포괄하는 개념 |
| **API 발전** | 초기 OpenAI API 용어 | 최신 표준 용어 (tools 매개변수) |
| **병렬 처리** | 제한적 | 병렬 도구 호출 지원 |
| **구현 방식** | 함수 중심 | 도구 생태계 중심 |

OpenAI는 2023년 6월 GPT-4와 GPT-3.5 Turbo에서 Function Calling을 발표했고, 이후 Tool Calling으로 확장 발전시켰습니다.
